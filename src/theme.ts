import { createTheme } from "@mui/material/styles"

const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#8a6534",
    },
    secondary: {
      main: "#f50057",
    },
    text: {
      primary: "#F3C057",
      secondary: "#E7F6F2",
    },
  },
})

export default theme
